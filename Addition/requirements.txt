# 合同读取智能体 - 依赖
# Anaconda(Py3.10+) 多数已自带，按需安装缺失项即可。

# —— 核心：运行必需 ——
requests>=2.31         # 调 OCR(PaddleOCR-VL) / DeepSeek 的 HTTP API
openpyxl>=3.0          # 写 Excel 台账
pydantic>=1.10         # 字段 schema（兼容 pydantic v1）
PyYAML>=6.0            # 读 config.yaml / field_map.yaml

# —— Web 服务：给前端提供 上传/实时进度(SSE)/历史/下载 接口 ——
fastapi>=0.110         # Web 框架
uvicorn[standard]>=0.27  # ASGI 服务器（uvicorn web.server:app）
python-multipart>=0.0.9  # 解析 multipart 上传（PDF）

# —— 测试：离线回归（无需 API key）——
pytest>=7.0

# —— 可选：仅当开启 config.yaml 的 enhance.cross_check_prices=true（§13.2 价格数字第二引擎交叉复核）才需要 ——
# 托管 OCR 路线正常运行不需要本地渲染，故以下默认不装。
# rapidocr_onnxruntime>=1.3   # 第二套本地 OCR 引擎
# PyMuPDF>=1.24               # 渲染价格表所在页
# opencv-python>=4.10         # 图像解码
# numpy>=1.24
