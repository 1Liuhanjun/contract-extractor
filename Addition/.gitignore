# —— 密钥/敏感信息（绝不入库）——
.env
*.key

# —— 运行产物 / 缓存（不是源码，体积大）——
output/
__pycache__/
*.pyc
.pytest_cache/
*.bak

# —— 用户投入的待处理文件（按需，避免把测试合同当源码）——
# input/*.pdf

# —— 前端依赖 / 构建产物（可从 package.json 重装，绝不入库）——
web/frontend/node_modules/
node_modules/
web/frontend/dist/

# —— 编辑器/系统 ——
.vscode/
.idea/
.DS_Store
Thumbs.db
